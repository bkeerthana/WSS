# Topic 05: Loops (for, while)

## Learning goals
- Use `for` loops for fixed iterations.
- Use `while` loops when the number of iterations depends on a condition.
- Build simple aggregations (e.g., sum).

## What students should do
Complete the TODOs in `starter.js`, then run:

```bash
node starter.js
```
