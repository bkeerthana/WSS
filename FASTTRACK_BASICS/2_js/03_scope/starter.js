/*
Activity 03: Scope (let, const, var)

Instructions:
1) Inside the first block, declare:
   - let x = 10
   - const y = 20
   Print x and y inside the block.

2) Outside the block, TRY printing x and y.
   - Keep the outside prints commented first.
   - Uncomment ONE line at a time to observe the ReferenceError.

3) Inside the second block, declare:
   - var z = 30
   Print z inside the block.

4) Print z outside the block and observe it works.
*/

{
  // TODO 1: Declare x and y

  // TODO 1: Print x and y inside block
}

// TODO 2: Uncomment one at a time to test scope
// console.log(x);
// console.log(y);

{
  // TODO 3: Declare var z and print it
}

// TODO 4: Print z outside the block
