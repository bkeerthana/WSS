/*
Activity 04: Conditional Statements

Instructions:
1) Create a variable marks (0 to 100). Some sample test values:
   - 95 (A)
   - 80 (B)
   - 65 (C)
   - 40 (D)
   - -5 (Invalid)
   - 120 (Invalid)

2) Print the grade using these rules:
   - marks >= 90 : "A"
   - marks >= 75 : "B"
   - marks >= 60 : "C"
   - else        : "D"

3) Add validation:
   - If marks is outside 0..100, print "Invalid marks"

Rules:
- Use if / else if / else.
*/

let marks = 72; // TODO: change this value to test multiple cases

// TODO: Write conditional logic here
